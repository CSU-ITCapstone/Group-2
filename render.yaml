services:
  - type: web
    name: printer-app
    env: docker
    repo: <YOUR_GITHUB_REPO_URL>
    branch: main
    buildCommand: dotnet publish --configuration Release --output out
    startCommand: dotnet out/PrinterApp.dll
