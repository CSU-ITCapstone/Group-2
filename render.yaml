services:
  - type: web
    name: Group 2 Printer App
    env: dotnet
    buildCommand: dotnet publish -c Release -o ./output
    startCommand: dotnet ./output/PrinterApp.dll
    publishDir: ./output
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
